import { NextRequest, NextResponse } from "next/server";
import { redis } from "@/lib/redis";

export const proxy = async <{connected; String[]; createdAt: number}>(`meta:${roomId}`);

  //if user allowed in the room, let them pass, if not, send to lobby
};

export const config = {
  macthcer: "/room/path*",
};
